# n8n runtime files

This directory is mounted read-only at `/files/n8n`. Workflows are intentionally stored in `../workflows` so they can be versioned and imported with `scripts/import-workflows.ps1`.
